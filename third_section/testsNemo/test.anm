var n : int;
var x : int;
var y : int;
{ (==(y, *(x, x)) & <=(-(x, 1()), n)) }
{
	n := 12;
	x := 0;
	y := 0;
	while (y <= n) inv { T } {
		y := (y + (x + (x + 1)));
		x := (x + 1)
	};
	x := (x - 1);
	print(x)
}
{ (<=(* (x, x), n) & >(*(+(1, x), +(1, x)), n))  }
